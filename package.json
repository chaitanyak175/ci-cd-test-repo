{
  "name": "ci-cd-test-repo",
  "version": "1.0.0",
  "description": "Test repository for CI/CD Fixer Agent",
  "main": "src/broken-app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/broken-app.js",
    "build": "echo \"No build process defined\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.0"
    // Missing comma - JSON syntax error
    "lodash": "^4.17.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
    // Missing dependency versions
    "prettier": "*"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  // Invalid JSON comment
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/ci-cd-test-repo.git"
  },
  "author": "CI/CD Fixer Agent Test",
  "license": "MIT"
}
